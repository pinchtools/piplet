<%= form_for(@users_user) do |f| %>
  <% if @users_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_user.errors.count, "error") %> prohibited this users_user from being saved:</h2>

      <ul>
      <% @users_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
