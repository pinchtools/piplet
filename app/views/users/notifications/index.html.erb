<div class="container">
  <%= render 'layouts/users/nav_bar' %>

  <div class="col-md-10 col-md-offset-1">
  
    <div class="row text-xs-right row-section">
      <%= link_to raw('<i class="fa fa-check"></i>') + t('users.notifications.index.mark-all-as-read-btn'), :unread_all_users_notifications, remote: true, class: 'btn btn-secondary' %>
    </div>
    
    
    <div class="row row-section">
    
      <%= will_paginate notifications , :previous_label => '', :next_label => '' %>
    
      <table class="table table-striped">
        <tbody>
          <%= render notifications %>
        </tbody>
      </table>
    
      <%= will_paginate notifications, :previous_label => '', :next_label => '' %>
    
    </div>
  </div>
  
</div>