<!-- Modal -->
<div class="modal fade" id="revertBlockModal" tabindex="-1" role="dialog" aria-labelledby="revertBlockModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <%= form_for :user, url: revert_block_admin_users_user_path, method: :post do |f| %>
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
          <h4 class="modal-title" id="revertBlockModalLabel"><%= t('admin.users.users.revert-block-modal.title') %></h4>
        </div>
        <div class="modal-body">
            <%= simple_format t("admin.users.users.revert-block-modal.notice") %>
        </div>
        <div class="modal-footer">
          <%= button_tag t('admin.users.users.revert-block-modal.close'), class: 'btn btn-default', 'type': 'button', 'data-dismiss': "modal" %>
          <%= button_tag t('admin.users.users.revert-block-modal.submit'), class: 'btn btn-primary' %>
        </div>
      <% end %>
      </div>
  </div>
</div>