<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.username, users_user_path(user) %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to t('global.destroy-btn'), [:users, user], method: :delete, data: { confirm: t('global.destroy-confirm') } %>
  <% end %>
</li>