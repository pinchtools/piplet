<%= form_tag :search_admin_users_users, method: :get do |f| %>
  <div class="input-group search-group">
  <input type="text" class="form-control" name="search" value= "<%= params[:search] %>" placeholder="<%= t 'admin.users.users.index.search_placeholder' %>">
  <span class="input-group-btn">
    <button class="btn btn-secondary" type="submit"><i class="fa fa-search" aria-hidden="true"></i></button>
  </span>
</div>
<% end %>